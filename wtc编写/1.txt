 从简单加权 → 选择性融合
之前 ： fused = 0.65 * wavelet + 0.35 * median ，全局加权导致中值滤波的平滑效果污染了小波去噪保留的细节。

现在 ：基于椒盐噪声检测的选择性融合：

1. 用 cv2.medianBlur 快速计算局部中值，与原始像素对比 → 差异 > 30 的像素被判定为椒盐噪声候选
2. 对噪声掩膜做高斯平滑，使融合边界自然过渡
3. 噪声区域 用中值修正， 其余区域 完全保留小波结果 — 小波去噪的效果不再被稀释
同时将 max_window 从 7 降为 5，减少自适应中值的过度平滑倾向。